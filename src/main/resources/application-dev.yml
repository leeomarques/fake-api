spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shalom
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        package-to-scan: com.example.fakeapi.infrastrucutre.entities
        dialect: org.hibernate.dialect.MySQLDialect
        show_sql: true # Isso mostrar� as consultas SQL no console para fins de depura��o
    open-in-view: false # Configura��o recomendada para evitar o problema de vazamento de sess�o
  cloud:
    loadbalancer:
      ribbon:
        enable: true
fake-api:
  url: "http://fakestoreapi.com/"

server:
  port: 8181
